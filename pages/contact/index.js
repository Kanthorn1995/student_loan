import React, { Component } from "react";
import ThemePublic from "../../components/theme_public";

class Contact extends Component {
  constructor(props) {
    super(props);
    this.state = {};
  }

  render() {
    return <div>Contact</div>;
  }
}

export default ThemePublic(Contact);
